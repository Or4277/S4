# PostgreSQL Console App (C#)

This project is a simple, structured C# console application that connects to PostgreSQL using `Npgsql` and dependency injection.

## Requirements
- .NET 8 SDK
- PostgreSQL server

## Configure
Edit `src/App/appsettings.json` and set the connection string.

## Run
From the project root:
- `dotnet run --project src/App`
